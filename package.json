{
  "name": "js_data_utils",
  "version": "0.0.1",
  "description": "Various functions for cleaning and manipulating data",
  "repository": {
    "type": "git",
    "url": "git://github.com/erictmc/js_data_utils.git"
  },
  "dependencies": {
    "moment-timezone": "^0.5.14"
  },
  "main": "./dist/index.js",
  "scripts": {
    "test": "jest --coverage",
    "pretest": "./node_modules/.bin/eslint .",
    "build": "babel src --presets babel-preset-env --out-dir dist",
    "preinstall": "npm install --ignore-scripts && babel src --presets babel-preset-env --out-dir dist"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-jest": "^22.2.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "eslint": "^4.17.0",
    "eslint-plugin-jest": "^21.7.0",
    "jest": "^22.2.1",
    "regenerator-runtime": "^0.11.1"
  }
}
